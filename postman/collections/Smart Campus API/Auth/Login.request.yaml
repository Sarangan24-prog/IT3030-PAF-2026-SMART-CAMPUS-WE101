$kind: http-request
method: POST
url: '{{baseUrl}}/api/auth/login'
order: 3000
headers:
  - key: 'Content-Type'
    value: 'application/json'
body:
  type: json
  content: |-
    {
      "email": "admin@example.com",
      "password": "Password123!"
    }
